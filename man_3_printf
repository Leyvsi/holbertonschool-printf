.\" Manpage for _printf
.\" Contact: Holberton School
.TH _PRINTF 3 "28 Nov 2025" "v1.0" "Holberton C Library"
.SH NAME
_printf \- simplified printf function
.SH SYNOPSIS
.B #include "main.h"
.br
.B int _printf(const char *format, ...);
.SH DESCRIPTION
The
.B _printf()
function is used to output characters to the standard output (stdout)
according to a predetermined format. It supports a limited set of format
specifiers and uses a
.I va_list
to access variable arguments.

The supported format specifiers are:
.TP
%% 
Prints a literal '%'.
.TP
%c
Prints a single character.
.TP
%s
Prints a string. If the pointer is
.RI NULL , "(null)" is printed.
.TP
%d
Prints a signed decimal integer.
.TP
%i
Prints a signed decimal integer.

Any unsupported specifier following '%' is printed literally.
Negative integers, including
.RI INT_MIN , are handled correctly.
.SH RETURN VALUE
On success,
.B _printf()
returns the number of characters printed. On failure, such as when
.I format
is
.RI NULL ,
it returns -1.
.SH EXAMPLES
.IP
.B _printf("Hello %s!\n", "World");
.br
Output:
Hello World!
.IP
.B _printf("Number: %d\n", -2147483648);
.br
Output:
Number: -2147483648
.IP
.B _printf("Percent: %%\n");
.br
Output:
Percent: %
.SH NOTES
This implementation does not support width, precision, flags, or length
modifiers. All output is written directly to stdout without internal
buffering.
.SH SEE ALSO
printf(3), write(2), va_start(3), va_arg(3), va_end(3)
